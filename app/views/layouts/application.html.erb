<!DOCTYPE html>
<html>
  <head>
    <title>LearnBlock</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet"  href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <!-- <link rel="stylesheet" type="text/css" href="/home/svyat-mlhv/project/ruby/LRNBLCK/app/assets/stylesheets/main.sass"> -->

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!-- <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous"> -->
    <div class="container-fluid">
      <nav class="navbar navbar-expand-lg navbar navbar-dark bg-dark" >
        <div class="container-fluid">
          <!-- <a class="navbar-brand" href="<%= root_path %>">Курсовая работа по дисциплине "Системное администрирование" Мелихов С.А. | ИЦТМС 3-3 | НИУ МГСУ 2021</a> -->
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
              <li class="nav-item <%= 'active' if current_page?(root_path) || current_page?(posts_path) %>">
                <a class="nav-link" href="<%= root_path %>">Курсовая работа по дисциплине "Системное администрирование"</a>
              <!-- </li>
              <% Category.order(:name).roots.each do |category| %>
                <li class="nav_item" <%= 'active' if current_page?(category_path(category))%>>
                  <%= link_to category.name, category, class: 'nav-link '%>
                </li>
              <% end %> -->
            </ul>
          </div>
        </div>
        <%# <%= link_to 'Новый раздел', new_post_path, class: 'btn btn-outline-primary pull-xs-right' %>
      </nav>
    </div>

    <div class="container main">
      <div class="row">
        <div class="col-md-12">
          <% flash.each do |name, message| %>
          <div class="alert alert-<%= name %>">
            <%= message %>
          </div>
          <% end %>
        </div>
      </div>

      <div class="row" >
        <div class="col-md-9">
          <%= yield %>
        </div>
        <div class="col-md-3 " >
          <div class="submenu sticky-top">
            <%= render 'shared/submenu' %>
          </div>

          <!-- <div class="tag_cloud ">
            <h2>Теги</h2>
            <% tag_cloud(Tag.counts, %w{tag1 tag2 tag3 tag4 tag5}) do |tag, css_class| %>
              <%= link_to tag.name, tag_path(tag.name), class: css_class %>
            <% end %>
          </div> -->
        </div>
      </div>

    </div>

    <footer id="footer" class="footer navbar-fixed-bottom"><!--Footer-->
        <div class="footer-bottom">
            <div class="container">
                <div class="row  btn  ">
                    <p class="pull-left" style="background-color: #D3D3D3;">© 2021 | Luvest</p>
                    <p class="pull-right" >Мелихов Святослав | ИЦТМС 3-3 | НИУ МГСУ</p>
                </div>
            </div>
        </div>
    </footer><!--/Footer-->

  </body>
</html>
